# /etc/conf.d/ipset

# Location in which ipset initscript will save set rules on 
# service shutdown
IPSET_SAVE="/var/lib/ipset/rules-save"

# Options to pass to ipset --save and ipset --restore 
SAVE_RESTORE_OPTIONS=""

# Save state on stopping ipset
SAVE_ON_STOP="yes"
